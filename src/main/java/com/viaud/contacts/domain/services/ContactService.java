package com.viaud.contacts.domain.services;

/**
 * Actually, this service provide no methods beacause there is no business rule for Contact except CRUD.
 * For CRUD, it's better to use directly ContactRepository.
 * 
 * @author Viaud
 */
public interface ContactService {
    
}
